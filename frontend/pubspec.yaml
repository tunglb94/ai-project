name: doctor_ai_app
description: "A new Flutter project for Doctor AI."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  go_router: ^14.0.0
  http: ^1.2.1
  flutter_bloc: ^8.1.5
  equatable: ^2.0.5
  flutter_secure_storage: ^9.2.2
  image_picker: ^1.1.2
  flutter_tts: ^4.0.0
  permission_handler: ^12.0.0
  path_provider: ^2.1.3
  google_mobile_ads: ^5.1.0
  google_fonts: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  # --- THÊM PHẦN NÀY VÀO ---
  assets:
    - assets/images/
  # -------------------------